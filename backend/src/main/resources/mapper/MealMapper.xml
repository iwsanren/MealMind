<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mealmind.mapper.MealMapper">

    <resultMap id="MealItemRowMap" type="com.mealmind.entity.MealItemRow">
        <id property="id" column="id"/>
        <result property="sourceType" column="source_type"/>
        <result property="ownerUserId" column="owner_user_id"/>
        <result property="name" column="name"/>
        <result property="mealTime" column="meal_time"/>
        <result property="mood" column="mood"/>
        <result property="scene" column="scene"/>
        <result property="healthGoal" column="health_goal"/>
        <result property="cuisine" column="cuisine"/>
        <result property="taste" column="taste"/>
        <result property="convenience" column="convenience"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="MealColumns">
        id, source_type, owner_user_id, name,
        meal_time, mood, scene, health_goal, cuisine, taste, convenience,
        created_at, updated_at
    </sql>

    <!-- created_at/updated_at set via NOW(); useGeneratedKeys returns the new id -->
    <insert id="insert" parameterType="com.mealmind.entity.MealItemRow"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO meal_item (
        source_type, owner_user_id, name,
        meal_time, mood, scene, health_goal, cuisine, taste, convenience,
        created_at, updated_at
        ) VALUES (
        #{sourceType}, #{ownerUserId}, #{name},
        #{mealTime}, #{mood}, #{scene}, #{healthGoal}, #{cuisine}, #{taste}, #{convenience},
        NOW(), NOW()
        )
    </insert>

    <!-- Ownership guard lives in WHERE: only a PERSONAL row owned by the caller is touched -->
    <update id="updatePersonal" parameterType="com.mealmind.entity.MealItemRow">
        UPDATE meal_item
        SET name = #{name},
        meal_time = #{mealTime},
        mood = #{mood},
        scene = #{scene},
        health_goal = #{healthGoal},
        cuisine = #{cuisine},
        taste = #{taste},
        convenience = #{convenience},
        updated_at = NOW()
        WHERE id = #{id}
        AND source_type = 'PERSONAL'
        AND owner_user_id = #{ownerUserId}
    </update>

    <delete id="deletePersonal">
        DELETE FROM meal_item
        WHERE id = #{id}
        AND source_type = 'PERSONAL'
        AND owner_user_id = #{userId}
    </delete>

    <select id="findPersonalById" resultMap="MealItemRowMap">
        SELECT <include refid="MealColumns"/>
        FROM meal_item
        WHERE id = #{id}
        AND source_type = 'PERSONAL'
        AND owner_user_id = #{userId}
    </select>

    <select id="findPersonalMeals" resultMap="MealItemRowMap">
        SELECT <include refid="MealColumns"/>
        FROM meal_item
        WHERE source_type = 'PERSONAL'
        AND owner_user_id = #{userId}
        ORDER BY updated_at DESC
    </select>

    <select id="findPublicMeals" resultMap="MealItemRowMap">
        SELECT <include refid="MealColumns"/>
        FROM meal_item
        WHERE source_type = 'PUBLIC'
        AND owner_user_id IS NULL
        ORDER BY updated_at DESC
    </select>

    <select id="countPersonalMeals" resultType="int">
        SELECT COUNT(1)
        FROM meal_item
        WHERE source_type = 'PERSONAL'
        AND owner_user_id = #{userId}
    </select>

    <!-- TODO (later step): <select id="search"> using JSON_OVERLAPS on the 7 tag columns. -->

</mapper>